

//this is the code related to assignment of arrow function that first letter of each string 
const Initials = (firstName, lastName) => {
    return `${firstName[0].toUpperCase()}${lastName[0].toUpperCase()}`;
  };
  
  
  console.log(Initials('Sai', 'prabhu')); 
  
